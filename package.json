{
  "name": "davao-routing",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint",
    "generate": "node ./build/generateMatrix.js",
    "start": "node ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dagrejs/graphlib": "^2.1.4",
    "@turf/turf": "^6.5.0",
    "big-json": "^3.1.0",
    "fastify": "^4.5.2"
  },
  "engines": {
    "node": ">=11.14.0"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  }
}
